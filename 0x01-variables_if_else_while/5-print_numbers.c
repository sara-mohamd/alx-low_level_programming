#include <stdio.h>
#include <stdlib.h>

/**
 * main -has codes prints alphabit in lowcase
 * Description: using main function
 * Return:0
*/

int main(void)
{
int i;
for (i = 0; i < 10; i++)
printf("%i", i);
putchar('\n');
return (0);
}
