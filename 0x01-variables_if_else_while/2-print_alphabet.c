#include <stdio.h>

/**
 * main -has codes prints alphabit in lowcase
 *
 * Description: using main function
 *
 * Return:0
*/

int main(void)
{
for (char i = 'a'; i <= 'z'; i++)

{
putchar(i);
}
putchar('\n');

return (0);
}
