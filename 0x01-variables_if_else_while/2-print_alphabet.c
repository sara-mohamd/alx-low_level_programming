#include <stdio.h>

/**
 * main -has codes prints alphabit in lowcase
 *
 * Description: using main function
 *
 * Return:0
*/

int main(void)
{
char i = 'a';
for (i = 'a'; i <= 'z'; i++)

{
putchar(i);
}
putchar('\n');

return (0);
}
