#include <stdio.h>
#include <stdlib.h>

/**
 * main -has codes prints alphabit in lowcase
* Description: using main function
 * Return:0
*/
int main(void)
{
char i = 'z';
for (i = 'z'; i >= 'a'; i--)
putchar(i);
putchar('\n');
return (0);
}
